# v0.7.5 Fix Notes 点货流程加图片上传

1. 电脑端货单列表显示字段，货号，商品名称，订单箱数，应到个数，Barcode，有效期，点货状态，实际来货个数，破损数
2. 电脑端货单列表增加详情入口，详情显示所有字段
3. 两端点货页面状态选择异常时，加备注和图片上传damageImgList。图片个数不超过三个，单个大小不超过5MB
4. 上传图片/api/damages和点货保存是两个接口，保存增加字段remark，damageImgList
5. 两端点货列表新增页面增加字段remark，damageImgList，逻辑同点货页面
6. 手机端点货列表通过状态增加BBD